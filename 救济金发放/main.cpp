#include<cstdio>
#include<algorithm>
#include<string>
#include<iostream>


int main(void)
{
    int all[20];
    int k;
    int m;
    int n;

    while(scanf("%d%d%d", &n, &k, &m)){
        if(n = 0 && k = 0 && m = 0) break;
        for(int j = n; n != 0; ){

        }
    }
}
